{
  "name": "rafael-news",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node scripts/build-stamp.mjs && node --test src/locales/*.test.mjs && astro check && astro build",
    "prebuild": "node scripts/build-stamp.mjs",
    "gate:web": "node scripts/gate-web.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/node": "^9.0.0",
    "pg": "^8.13.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "@types/pg": "^8.11.0"
  }
}
